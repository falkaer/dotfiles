Install the cnrdrvcups-lb(-bin) package and set up printer with UFR II, and point it to the .ppd file in this directory.

CUPS overwrites the .ppd file when setting up so it will break symlinks, have to do it manually.
